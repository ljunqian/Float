declare type DrawerProps = {
    placement?: 'top' | 'left' | 'right' | 'bottom';
    children?: any;
    isOpen: boolean;
    onClose?: () => void;
};
declare const Drawer: ({ children, isOpen, onClose, placement, }: DrawerProps) => JSX.Element | null;
export default Drawer;
