type User @model @auth(rules: [{allow: private}]) {
  id: ID!
  name: String!
  email: String!
  coins: Int!
  meditateD: AWSTime!
  sleepD: AWSTime!
  moveD: AWSTime!
  focusD: AWSTime!
  friends: [String!]
}
 